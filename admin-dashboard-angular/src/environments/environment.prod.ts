export const environment = {
  production: true,
  // Same Railway service as the API — use relative URLs.
  apiBase: '',
  platformPercentage: 10,
  allowedAdminEmail: 'mohamed.razzak111@gmail.com',
};
